<header class="bg-gradient-to-r from-indigo-600 to-purple-600 text-white p-3 mb-4">
    <div class="container d-flex justify-content-between align-items-center">
        <h1 class="h4 m-0">Samuel Yang's Note App</h1>
        <nav>
            <ul class="nav">
                <% if (currentUser) { %>
                    <li class="nav-item"><a class="nav-link text-white" href="/notes">Home</a></li>
                    <li class="nav-item">
                        <form action="/logout" method="POST" class="d-inline">
                            <button class="btn btn-warning btn-sm">Logout</button>
                        </form>
                    </li>
                <% } else { %>
                    <li class="nav-item"><a class="nav-link text-white" href="/login">Login</a></li>
                    <li class="nav-item"><a class="nav-link text-white" href="/signup">Signup</a></li>
                <% } %>
            </ul>
        </nav>
    </div>
</header>
