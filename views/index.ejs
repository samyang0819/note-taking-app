<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>My Notes - Dashboard</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="/css/styles.css">
<style>
    body { background-color: #f0f4f8; font-family: 'Segoe UI', sans-serif; }

    h1.dashboard-title {
        text-align: center;
        margin-top: 30px;
        margin-bottom: 20px;
        font-size: 2.5rem;
        color: #111827;
    }

    .hero-section {
        width: 100%;
        height: 50vh; 
        overflow: hidden;
        border-bottom-left-radius: 20px;
        border-bottom-right-radius: 20px;
        position: relative;
        margin-bottom: 40px;
    }

    .hero-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
        transition: transform 0.5s ease;
    }

    .hero-section:hover .hero-img {
        transform: scale(1.05);
    }

    .add-note-btn {
        position: absolute;
        bottom: 20px;
        right: 20px;
        background-color: #FBBF24;
        color: #111827;
        font-weight: bold;
        padding: 12px 24px;
        border-radius: 12px;
        box-shadow: 0 6px 12px rgba(0,0,0,0.15);
        transition: transform 0.2s, box-shadow 0.2s;
        text-decoration: none;
        z-index: 10;
    }

    .add-note-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 16px rgba(0,0,0,0.2);
        color: #111827;
    }

    .note-card {
        border-radius: 12px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        transition: transform 0.2s, box-shadow 0.2s;
    }

    .note-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 6px 15px rgba(0,0,0,0.15);
    }

    .note-color-0 { background-color: #ffd6d6; } 
    .note-color-1 { background-color: #fff0d6; } 
    .note-color-2 { background-color: #d6ffd8; } 
    .note-color-3 { background-color: #d6f7ff; } 
    .note-color-4 { background-color: #f0d6ff; } 

    @media (min-width: 768px) { .hero-section { height: 60vh; } }
    @media (min-width: 1200px) { .hero-section { height: 70vh; } }
</style>
</head>
<body>

<%- include('partials/header') %>

<main class="container mt-4">
    <h1 class="dashboard-title">My Notes</h1>

    <div class="hero-section">
        <img src="/images/sticky-notes.jpeg" alt="Dashboard Banner" class="hero-img">
        <a href="/notes/new" class="add-note-btn">+ Add New Note</a>
    </div>

    <% if (notes.length === 0) { %>
        <p class="text-center mt-4">You have no notes. Click the button above to add one!</p>
    <% } %>

    <div class="row mt-4">
        <% notes.forEach((note, i) => { %>
            <div class="col-md-4">
                <div class="card note-card p-3 note-color-<%= i % 5 %> mb-3">
                    <div class="card-body">
                        <h5 class="card-title"><%= note.title %></h5>
                        <p class="card-text"><%= note.content.substring(0, 100) %>...</p>
                        <a href="/notes/<%= note._id %>" class="btn btn-primary w-100">View</a>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</main>

<%- include('partials/footer') %>
</body>
</html>
